name: talos-aws-pulumi_python
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: An AWS + Talos k8s cluster
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
